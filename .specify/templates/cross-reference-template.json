{
  "$schema": "./cross-reference-schema.json",
  "version": "1.0",
  "projectName": "[PROJECT_NAME]",
  "updated": "[DATE]",

  "_comment_screens": "Screen-level cross-references. Define which M-*/API-* each screen uses. Created during /speckit.design.",
  "screens": {
    "SCR-001": {
      "name": "[Screen Name]",
      "masters": ["M-XXX"],
      "apis": ["API-XXX-ACTION"]
    }
  },

  "_comment_features": "Feature-level cross-references. Define which SCR-*/M-*/API-*/BR-* each feature uses. Created during /speckit.issue or /speckit.add.",
  "features": {
    "S-FOUNDATION-001": {
      "title": "基盤構築",
      "screens": ["SCR-001"],
      "masters": ["M-XXX"],
      "apis": ["API-XXX-ACTION"],
      "rules": []
    }
  },

  "_comment_permissions": "Role-based permissions for APIs. Optional but recommended.",
  "permissions": {
    "API-XXX-ACTION": ["role1", "role2"]
  }
}
